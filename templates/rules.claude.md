## project-brain MCP

You have access to the `project-brain` MCP server for codebase knowledge retrieval.

{{tools}}

When answering questions about the codebase, run `search_context` first for fuzzy/conceptual questions, and use the structural tools (`find_symbol`/`find_callers`/`find_callees`/`impact`) for exact symbols, callers, and change impact — before reading files directly.
