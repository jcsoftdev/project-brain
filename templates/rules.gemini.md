## project-brain MCP

You have access to `project-brain` MCP tools for codebase knowledge retrieval.

{{tools}}

When answering codebase questions, prefer `search_context` for fuzzy/conceptual lookups and the structural tools for exact symbols, callers, and change impact — faster and more precise than reading files directly.
